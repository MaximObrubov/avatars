<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>App</title>
  </head>
  <body>
    {{ message }}

    <img src="{{image_test}}" alt="">

    {{#if image}}
      <img src="{{image}}" alt="">
    {{else}}
      No image
    {{/if}}
  </body>
</html>
