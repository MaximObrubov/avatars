<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>App</title>
    <style>
      body {
        margin: 0 auto;
        width: 960px;
        text-align: left;
        padding: 2em;
        font-family: Helvetica;
        color: #444;
      }

      .avatar {
        margin: 2em 0;
      }
    </style>
  </head>
  <body>
    <h1>{{ message }}</h1>

    <form action="/change-name" method="post">
      <input type="text" name="name" value="{{name}}" />
      <input type="submit" />
    </form>

    <h4>hello, {{ name }}</h4>

    {{#if name}}
      {{!-- TODO not working alias for container --}}
      {{!-- <img src="{{image}}" alt=""> --}}
      {{!-- <img class="avatar" src="http://localhost:8080/{{image_path}}" alt=""> --}}
      <img class="avatar" src="{{image}}" alt="">
    {{else}}
      No image
    {{/if}}
  </body>
</html>
